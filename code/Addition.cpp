    #include <iostream>
     
    using namespace std;
     
    int main()
    {
       int a, b;
       
       cout << "Enter two integers to add\n";
       cin >> a >> b;  //this is called cascading
     
      /* c = a + b;  --no need to create extra variables, it consumes memory */
       cout <<"Sum of the numbers: " << c << endl;
       
       return 0;
    }
